# KNN
K-Nearest Neighbors (KNN) is a simple "lazy" algorithm for classification and regression. It predicts a new data point's label by finding the 'K' most similar stored instances and using a majority vote or average. Its performance depends heavily on choosing the right K and scaling the data.
